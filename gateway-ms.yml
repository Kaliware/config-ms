spring:
  application:
    name: gateway-ms
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://AUTH-MS  # Nome correto do serviço no Eureka
          predicates:
            - Path=/auth-ms/**  # Roteia todas as requisições que começam com /auth-ms
server:
  servlet:
    contextPath: /gateway
